- content_for :breadcrumb do
  = link_to 'Home', root_path
  .breadcrumb-seperator /
  = link_to 'Bins', bins_path
  .breadcrumb-seperator /
  = link_to @bin.name, @bin
  .breadcrumb-seperator /

- content_for :page_title do
  Edit Bin

= render :partial => 'form', :object => @bin
