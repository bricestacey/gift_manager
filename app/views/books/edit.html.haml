- content_for :breadcrumb do
  = breadcrumbs('Home' => root_path, 'Books' => books_path, @book.title => @book)

- content_for :page_title do
  Edit Book

= render :partial => 'form', :object => @book
